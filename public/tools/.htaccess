# Bloque tout sauf localhost (Apache 2.4+)
Require local

# Compatibilit? Apache 2.2 (si jamais)
<IfModule !mod_authz_core.c>
  Order Deny,Allow
  Deny from all
  Allow from 127.0.0.1
  Allow from ::1
</IfModule>
